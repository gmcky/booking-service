info:
  name: Property Review Stats
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/reviews/property/{{propertyId}}/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
