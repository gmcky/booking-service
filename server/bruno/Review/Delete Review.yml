info:
  name: Delete Review
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/reviews/{{reviewId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
