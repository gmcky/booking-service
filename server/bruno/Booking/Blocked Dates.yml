info:
  name: Blocked Dates
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/bookings/{{propertyId}}/blocked-dates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
