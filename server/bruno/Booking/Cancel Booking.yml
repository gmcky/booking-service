info:
  name: Cancel Booking
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/bookings/{{bookingId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
