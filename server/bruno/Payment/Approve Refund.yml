info:
  name: Approve Refund
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/payments/{{paymentId}}/refund/approve"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
