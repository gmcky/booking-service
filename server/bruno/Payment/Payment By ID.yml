info:
  name: Payment By ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/payments/{{paymentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
