info:
  name: Process Payment
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/payments/{{paymentId}}/process"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
