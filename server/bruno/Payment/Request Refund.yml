info:
  name: Request Refund
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/payments/{{paymentId}}/refund"
  body:
    type: json
    data: |-
      {
        "reason": "refund reason text 12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
