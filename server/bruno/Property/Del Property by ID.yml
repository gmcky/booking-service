info:
  name: Del Property by ID
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/properties/{{testProperty}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
