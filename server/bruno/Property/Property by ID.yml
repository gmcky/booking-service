info:
  name: Property by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/properties/{{propertyId2}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
